[package]
name = "encrypt"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22.1"
clap = { version = "4.6.1", default-features = false, features = ["std", "help", "usage", "error-context"] }
